// Loaded via require() in ucode program mode, not ES-module mode — export is
// unavailable; exports are a bare `return` at module scope.
return {
	greet: function() {
		return "REAL (lib_paths)";
	}
};
